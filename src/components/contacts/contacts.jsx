import styles from './contacts.module.css';

const Contacts = () => {
    return (
        <article className={styles.wrap}>
            <h1 className={styles.header}>
                Contacts
            </h1>
        </article>
    );
};

export { Contacts };